# Battle of Britain — Linux Port: Session Status 2026-07-01 (S72 → S82)

Branch: `linux-port`. Build: 32-bit i386 ELF (`gcc -m32`, `-fpack-struct=1`), SDL2 + OpenGL + OpenAL.
Game sources stay unedited except `#if BOB_LINUX` guards; the port lives in `SRC/compat/` + the `BOB_*`
env-gated boot scaffolds. Full dated evidence is in **PORT.md** (newest-first); this is the summary.

This session ran a scrum continuing the PO-chosen **historic-missions** epic to completion, fixed two
PO-reported in-flight rendering issues, and opened the **full-campaign** epic (scoping + Phase-1 start).

## Headline
- **All 7 historic quick missions (23–29) now fly** — was only 24 & 26 at the start of the epic.
- **Double-exposure aircraft — FIXED.** Your squadron flies as a spread formation, not a 1-metre stack.
- **Cockpit clouds z-fighting — FIXED.** The cockpit renders solid with clouds correctly behind the glass.
- **The whole quick-mission fleet (0–29 sampled) flies and every reachable path is ASan-clean.**
- **Full-campaign epic opened:** scoped with a phased plan; Phase 1 started (flow reachable, art renders,
  next seam pinpointed).

## Historic-QM epic — crash arc complete (fixes 3/N & 4/N)
| Sprint | Fix |
|---|---|
| **S72** `3cfb85b` | **QM 28 flies.** Its player is squadron 73 = the **Defiant** (`DUMMY_Setup`, no `THRUSTPOINT`) → SEGV in the manual flight model. The real frag screen never lets you fly a non-flyable type; the scaffold bypassed that filter. Fix (`Persons3::FinishSetPiece`): reassign the player to a flyable aircraft the mission actually built (QM 28 is a Luftwaffe raid → a Bf109). Also fixed 2 ASan overflows this exposed: off-map ground-grid (`Grid_Base::getWorld` clamp) + player anim buffer (`SetPilotedAcAnim`). |
| **S74** `62d3e4e` | **QM 23 & 25 fly.** Out-of-range player squadron (168/208 > `SQ_MAX`) → no player AC built → "No player A/C" FATAL. Extended the S72 reassignment to the NULL-player case. |

## Double-exposure ("two aircraft superimposed") — root-caused then FIXED
Traced across five investigation sprints (S73/S75/S76/S77) to the exact field, then fixed:
| Sprint | Result |
|---|---|
| S73 `3ea5b71` | Ruled out the Ghost/Seen predictor split; added `BOB_TRACE_PROX`; found formation-mates at ~1 m. |
| S75 `fe318d4` | Pinned it: every scramble aircraft has `formation==0` → identical zeroth waypoint offset. |
| S76 `b57cf04` | Bounded `COURSEPOS` fix tried → insufficient (degenerate scramble waypoints); reverted. |
| S77 `bdbe938` | Narrowed to **flight leaders** converging (wingmen already formate); confirmed at cruise. |
| **S78** `3d5a2d4` | **FIX.** `Item::Formation_xyz` reads `FormationType::wingpos[16]` but the tables define only ~4 flight positions; the scaffold over-fills one scramble squadron with ~15 flights, so surplus flights read a zero-padded slot → `{0,0,0}` offset → they stack on the squadron leader (you). Synthesise an altitude-staggered echelon for undefined slots. **Nearest aircraft ~1 m → 13–209 m.** Inert for campaign (squadrons never exceed their table). |

## Cockpit cloud z-fighting — FIXED
| Sprint | Result |
|---|---|
| **S81** `e678fe6` | Compat reports a 32-bit Z-buffer, so the game skips its native <24-bit cockpit depth-flush — but the GL depth still z-fights the near cockpit against the far cloud layer, hazing the pit. Fix (`ThreeDee::render3d`): always `FlushAsBackground` before the cockpit on Linux (`_BeginScene` clears only Z, preserving the scene colour, so the cockpit draws on top). Verified before/after; ASan-clean. Added `BOB_TRACE_ZDEPTH`. |

## Validation
- **All 7 historic QMs (23–29) ASan-clean** (S72/S74/S78/S80 passes); **75 s soak of QM 26 clean** (S79).
- **Combat/training QM fleet 0–22 sampled: all fly** (S81), no regression from the session's core-path changes.
- Formation spread validated across the fleet (nearest-AC 13–209 m). Plain/bare `./bob` exit 0.
- Cross-port: S72→S78 addendum appended to the shared engine notes flagging the two ENGINE-shared finds for
  the MiG Alley port (`Formation_xyz` unclamped `wingpos[]`, `getWorld` unclamped grid index).

## Full-campaign epic (PO-added) — scoped + Phase-1 started
- **Scoped (S82 `8c3f7d0`):** the campaign map subsystem is real, partly-ported game code — `CMIGView`
  (MIGVIEW.CPP, 4020 lines, 17 `BOB_LINUX` sites), `MAPDLG.CPP`, `MSCTLBR.CPP` toolbar, `Todays_Packages` OOB,
  `Campaign::InitIcons`. Phased plan: (1) reach + render the map, (2) icons + toolbars, (3) interaction,
  (4) campaign loop (day advance / SAG sim / debrief / save-load).
- **Phase 1 started (`b6431c6`/`e710c83`):** the campaign flow is reachable from the real front-end
  (`BOB_FRONTEND` + `BOB_AUTOCLICK=1`). The main menu is fully working (title art + navigable menu); the
  Campaigns screen's **background art renders but its menu/controls don't** — traced to `bob_draw_menu`
  iterating `scr->textlists[]`, which is empty for that screen. **Next task:** the campaign screens use a
  non-`textlists` control model; identify and render their controls (Phase 2 = the PO's "controls + icons").

## Open backlog
1. **Full campaign implementation** (map controls + icons) — the large epic above; continue at Phase 2.
2. **External F6-view z-fighting** — separate from the cockpit (no cockpit there); the *scene* depth-precision
   issue (clouds vs terrain/aircraft), a near/far-plane fix. Headless external-view capture is currently
   blocked (outside-view present path doesn't advance frames in the test harness) — a lead to clear first.

## This session's commits (S72 → S82, 15 total)
`3cfb85b` S72 QM28 · `3ea5b71` S73 · `62d3e4e` S74 QM23/25 · `8323691` S73-fu · `fe318d4` S75 ·
`b57cf04` S76 · `bdbe938` S77 · `3d5a2d4` S78 double-exposure FIX · `b21f3c8` S79 soak ·
`77876b0` S80 fleet ASan · `6827b68` backlog · `e678fe6` S81 z-fighting FIX · `8c3f7d0` S82 scope ·
`b6431c6` S82 Phase-1 · `e710c83` S82 Phase-1 sharpen.
