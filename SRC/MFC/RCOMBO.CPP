/*
	 Battle of Britain
	 Copyright (C) 2000, 2001 Empire Interactive (Europe) Ltd,
	 677 High Road, North Finchley, London N12 0DA

	 Please see the document licence.doc for the full licence agreement

2. LICENCE
 2.1 	
 	Subject to the provisions of this Agreement we now grant to you the 
 	following rights in respect of the Source Code:
  2.1.1 
  	the non-exclusive right to Exploit  the Source Code and Executable 
  	Code on any medium; and 
  2.1.2 
  	the non-exclusive right to create and distribute Derivative Works.
 2.2 	
 	Subject to the provisions of this Agreement we now grant you the
	following rights in respect of the Object Code:
  2.2.1 
	the non-exclusive right to Exploit the Object Code on the same
	terms and conditions set out in clause 3, provided that any
	distribution is done so on the terms of this Agreement and is
	accompanied by the Source Code and Executable Code (as
	applicable).

3. GENERAL OBLIGATIONS
 3.1 
 	In consideration of the licence granted in clause 2.1 you now agree:
  3.1.1 
	that when you distribute the Source Code or Executable Code or
	any Derivative Works to Recipients you will also include the
	terms of this Agreement;
  3.1.2 
	that when you make the Source Code, Executable Code or any
	Derivative Works ("Materials") available to download, you will
	ensure that Recipients must accept the terms of this Agreement
	before being allowed to download such Materials;
  3.1.3 
	that by Exploiting the Source Code or Executable Code you may
	not impose any further restrictions on a Recipient's subsequent
	Exploitation of the Source Code or Executable Code other than
	those contained in the terms and conditions of this Agreement;
  3.1.4 
	not (and not to allow any third party) to profit or make any
	charge for the Source Code, or Executable Code, any
	Exploitation of the Source Code or Executable Code, or for any
	Derivative Works;
  3.1.5 
	not to place any restrictions on the operability of the Source 
	Code;
  3.1.6 
	to attach prominent notices to any Derivative Works stating
	that you have changed the Source Code or Executable Code and to
	include the details anddate of such change; and
  3.1.7 
  	not to Exploit the Source Code or Executable Code otherwise than
	as expressly permitted by  this Agreement.

questions about this file may be asked at bob@rowansoftware.com a
better place to ask is http://www.simhq.com/ or even :-
http://www.simhq.com/cgi-bin/boards/cgi-bin/forumdisplay.cgi?action=topics&forum=Battle+of+Britain&number=40&DaysPrune=20&LastLogin=
*/

// Machine generated IDispatch wrapper class(es) created by Microsoft Visual C++

// NOTE: Do not modify the contents of this file.  If this class is regenerated by
//  Microsoft Visual C++, your modifications will be overwritten.


#include "stdafx.h"
#include "rcombo.h"

/////////////////////////////////////////////////////////////////////////////
// CRCombo

IMPLEMENT_DYNCREATE(CRCombo, CWnd)

/////////////////////////////////////////////////////////////////////////////
// CRCombo properties

OLE_COLOR CRCombo::GetForeColor()
{
	OLE_COLOR result;
	GetProperty(DISPID_FORECOLOR, VT_I4, (void*)&result);
	return result;
}

void CRCombo::SetForeColor(OLE_COLOR propVal)
{
	SetProperty(DISPID_FORECOLOR, VT_I4, propVal);
}

CString CRCombo::GetCaption()
{
	CString result;
	GetProperty(DISPID_CAPTION, VT_BSTR, (void*)&result);
	return result;
}

void CRCombo::SetCaption(LPCTSTR propVal)
{
	SetProperty(DISPID_CAPTION, VT_BSTR, propVal);
}

BOOL CRCombo::GetEnabled()
{
	BOOL result;
	GetProperty(DISPID_ENABLED, VT_BOOL, (void*)&result);
	return result;
}

void CRCombo::SetEnabled(BOOL propVal)
{
	SetProperty(DISPID_ENABLED, VT_BOOL, propVal);
}

long CRCombo::GetFontNum()
{
	long result;
	GetProperty(0x1, VT_I4, (void*)&result);
	return result;
}

void CRCombo::SetFontNum(long propVal)
{
	SetProperty(0x1, VT_I4, propVal);
}

long CRCombo::GetListboxLength()
{
	long result;
	GetProperty(0x2, VT_I4, (void*)&result);
	return result;
}

void CRCombo::SetListboxLength(long propVal)
{
	SetProperty(0x2, VT_I4, propVal);
}

BOOL CRCombo::GetCircularStyle()
{
	BOOL result;
	GetProperty(0x3, VT_BOOL, (void*)&result);
	return result;
}

void CRCombo::SetCircularStyle(BOOL propVal)
{
	SetProperty(0x3, VT_BOOL, propVal);
}

unsigned long CRCombo::GetShadowColor()
{
	unsigned long result;
	GetProperty(0x4, VT_I4, (void*)&result);
	return result;
}

void CRCombo::SetShadowColor(unsigned long propVal)
{
	SetProperty(0x4, VT_I4, propVal);
}

long CRCombo::GetEndFileNum()
{
	long result;
	GetProperty(0x5, VT_I4, (void*)&result);
	return result;
}

void CRCombo::SetEndFileNum(long propVal)
{
	SetProperty(0x5, VT_I4, propVal);
}

long CRCombo::GetFileNumMain()
{
	long result;
	GetProperty(0x6, VT_I4, (void*)&result);
	return result;
}

void CRCombo::SetFileNumMain(long propVal)
{
	SetProperty(0x6, VT_I4, propVal);
}

long CRCombo::GetStyle()
{
	long result;
	GetProperty(0x7, VT_I4, (void*)&result);
	return result;
}

void CRCombo::SetStyle(long propVal)
{
	SetProperty(0x7, VT_I4, propVal);
}

long CRCombo::GetNewJimVar()
{
	long result;
	GetProperty(0x8, VT_I4, (void*)&result);
	return result;
}

void CRCombo::SetNewJimVar(long propVal)
{
	SetProperty(0x8, VT_I4, propVal);
}

/////////////////////////////////////////////////////////////////////////////
// CRCombo operations

void CRCombo::AddString(LPCTSTR text)
{
	static BYTE parms[] =
		VTS_BSTR;
	InvokeHelper(0x9, DISPATCH_METHOD, VT_EMPTY, NULL, parms,
		 text);
}

long CRCombo::GetListbox()
{
	long result;
	InvokeHelper(0xa, DISPATCH_METHOD, VT_I4, (void*)&result, NULL);
	return result;
}

void CRCombo::SetIndex(long row)
{
	static BYTE parms[] =
		VTS_I4;
	InvokeHelper(0xb, DISPATCH_METHOD, VT_EMPTY, NULL, parms,
		 row);
}

#if defined(BOB_LINUX)
/* S310 (bob): set by CRCombo::GetIndex when the OLE dispatch went unanswered; read by SG2COMBO.H's
   write-back. One flag suffices because the write-back reads it immediately after the call it
   belongs to, in the same statement sequence. */
int g_bob_combo_unanswered = 0;   /* declared in SG2COMBO.H without a linkage spec - see there */
extern int g_bob_ole_unanswered;  /* S311: raised by bob_ole_invoke when no host answered */
#endif

long CRCombo::GetIndex()
{
	long result;
#if defined(BOB_LINUX)
	/* S289: this returned an UNINITIALISED long whenever nothing answered the dispatch.
	   bob_ole_invoke is `if (h) h->dispatch(...)` -- with no registered OLE host for the control it
	   never writes pvRet, and this function hands the caller whatever was on the stack.
	   That matters because of who calls it: SETFIELD in SG2COMBO.H does `val = rcombo->GetIndex()`
	   and then writes val's BITS into Save_Data.gamedifficulty. So an unanswered dispatch does not
	   fail visibly -- it silently sets or clears real preferences, including
	   GD_GUNCAMERAATSTART, the one that decides whether a sortie is recorded at all.
	   Sentinel first, verdict second: BOB_TRACE_COMBO=1 reports whether anything answered. The
	   fallback to 0 is NOT the fix, it is damage control -- deterministic beats random -- and 0 is
	   itself a guess ("first item"), which is why it is traced rather than left silent. */
	/* S311: ask the ROUTER whether anyone answered, instead of relying on a sentinel surviving.
	   bob_ole_invoke now zeroes the return buffer on an unanswered dispatch (so that the other 30
	   value-returning wrappers stop reading the stack), which would have overwritten -0x5EED and
	   silently disabled the guard below. A sentinel only works while nothing else writes that slot;
	   a flag from the code that knows the answer does not have that failure mode. */
	result = -0x5EED;
	InvokeHelper(0xc, DISPATCH_METHOD, VT_I4, (void*)&result, NULL);
	int unanswered = g_bob_ole_unanswered || (result == -0x5EED);
	if (getenv("BOB_TRACE_COMBO"))
		fprintf(stderr, "[combo] GetIndex -> %ld%s\n", result,
		        unanswered ? "   *** NOBODY ANSWERED: value would have been stack garbage" : "");
	/* S310 (bob): tell the CALLER that nobody answered, instead of only guessing for it.
	   The 0 below is still a guess and still wrong; what was missing was any way for the one caller
	   that matters to know it is being handed one. SETFIELD's write-back now reads this flag and
	   SKIPS the write rather than storing a fabricated index into Save_Data -- leaving a preference
	   alone is always defensible, inventing one never is. */
	/* S310 POSITIVE CONTROL. The guard below only matters when a dispatch goes unanswered, and in
	   a healthy run that never happens -- bob_settings_nav reports "combo dispatches returning
	   stack garbage: 0". So the guard's correct behaviour is unobservable without forcing the
	   condition, and an unobservable guard is indistinguishable from no guard at all.
	   BOB_COMBO_FORCE_UNANSWERED=1 makes every GetIndex report unanswered. */
	if (getenv("BOB_COMBO_FORCE_UNANSWERED")) unanswered = 1;
	g_bob_combo_unanswered = unanswered;
	if (unanswered) result = 0;
	return result;
#else
	InvokeHelper(0xc, DISPATCH_METHOD, VT_I4, (void*)&result, NULL);
	return result;
#endif
}

void CRCombo::Clear()
{
	InvokeHelper(0xd, DISPATCH_METHOD, VT_EMPTY, NULL, NULL);
}

void CRCombo::DeleteString(long index)
{
	static BYTE parms[] =
		VTS_I4;
	InvokeHelper(0xe, DISPATCH_METHOD, VT_EMPTY, NULL, parms,
		 index);
}
